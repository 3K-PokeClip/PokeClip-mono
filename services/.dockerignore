build/
*/build/
.gradle/
*.iml
.idea/

# 시크릿. 빌드 컨텍스트가 services/ 라 COPY . . 가 이걸 빌드 스테이지로 들여온다.
# 지금은 멀티스테이지라 최종 이미지에 안 남지만, 그 안전은 Dockerfile 구조에
# 기대는 것이다 — 단일 스테이지로 바꾸거나 스테이지를 레지스트리에 올리는 날
# 조용히 유출된다. 컨텍스트에 아예 안 넣는다.
.env
.env.*
